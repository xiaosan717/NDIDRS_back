server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dorm_check_db?useUnicode=true&charset=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
  mail:
    # 使用QQ邮箱发送验证码
    host: smtp.qq.com
    port: 465
    username: 3031114349@qq.com
    password: pqvcrhdulgoydgii
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
            port: 465

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: dorm-check-system-jwt-secret-key-2024
  expiration: 86400000

logging:
  level:
    com.dorm.ndidrs_back: debug